# ⚠️ SCRATCH — Not the approved implementation

This directory was created as part of the same scratch attempt described
in `workout-priority-dashboard/SCRATCH.md`. It is empty and was never
populated with the real integration build.

**Do not treat this as the source of truth.**

The canonical `iron-logic-integrated/` will come from the user's
Perplexity Computer workspace export.

Status: waiting for source package.
